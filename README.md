# Note App

```bash
yarn add apollo-cache-inmemory apollo-client graphql styled-components styled-reset react-textarea-autosize graphql-tag apollo-link-state react-router-dom apollo-link react-apollo
```
